# credential-bearing directories — NEVER commit
gh/
github-copilot/
configstore/
gnupg/
gpg/

# machine-local noise / rebuildable binaries
.DS_Store
wallpapers/

# self-contained app repos with their own remotes (nested git repos)
simple-bar/
simple-bar-server/

# Zed LMDB runtime databases (lock file churns constantly)
zed/prompts/*.mdb/
